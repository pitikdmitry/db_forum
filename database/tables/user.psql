CREATE SEQUENCE userTableSeq;

CREATE TABLE userTable(
user_id INTEGER PRIMARY KEY default nextval('userTableSeq'),
about TEXT NOT NULL,
email TEXT NOT NULL UNIQUE,
fullname TEXT NOT NULL,
nickname TEXT NOT NULL UNIQUE
);